fe + be

pnpm dev


just terminal agent

pnpm agent:py:console


after change lead data:

pnpm moss:index




pnpm dev:backend    # FastAPI hub on :8000
pnpm dev:frontend   # Next.js on :3000  → /dashboard
pnpm dev:agent-py   # LiveKit agent worker (dev mode, picks up dispatched calls)

